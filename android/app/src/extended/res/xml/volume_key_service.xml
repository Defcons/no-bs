<?xml version="1.0" encoding="utf-8"?>
<!-- EXTENDED flavour only. flagRequestFilterKeyEvents + canRequestFilterKeyEvents are
     what let onKeyEvent see volume presses globally (locked screen, music playing). -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/volume_key_service_desc"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRequestFilterKeyEvents"
    android:canRequestFilterKeyEvents="true"
    android:notificationTimeout="100" />
